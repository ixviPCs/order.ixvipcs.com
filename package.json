{
  "name": "stripe-sample",
  "version": "0.1.0",
  "dependencies": {
    "@stripe/stripe-js": "^8.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^20.1.0",
    "vite": "^5.2.11"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "scripts": {
    "dev": "concurrently \"vite --port 3000 --open\" \"node server.js\"",
    "build": "vite build"
  }
}

